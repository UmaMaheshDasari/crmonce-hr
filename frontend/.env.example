# Frontend environment variables
# Copy to .env:  cp .env.example .env

# API URL — with Vite proxy (recommended for local dev)
VITE_API_URL=http://localhost:5000/api

# OR if using Vite proxy (vite.config.js already sets this up):
# VITE_API_URL=/api
